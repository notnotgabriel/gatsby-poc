import React from 'react'
import Header from '../components/header'

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerTitle="About" />
    <Header headerTitle="Duplicated" />
    <p>Such wow. Very React.</p>
  </div>
)
